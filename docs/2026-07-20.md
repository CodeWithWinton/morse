# 2026-07-20 Daily Log

## Work Done
* Initial project kickoff for [[Morse - Master Hub]].
* Attempted accelerometer hardware streaming via `macimu` and `hidapi`.
* Enumerated HID devices (`enumerate_hid.py`) and ran `sudo python3 stream_data.py`.
* Executed `ioreg` kernel diagnostics. Discovered base M1 MacBook Air lacks `accel` and `gyro` nodes on the SPU.
* Documented hardware limitations in [[Hardware Discovery]].

Back to [[Morse - Master Hub]]
